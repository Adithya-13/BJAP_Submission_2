<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MovieAppJetpackSubmission2" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/redBlack</item>
        <item name="colorOnPrimary">@color/grey</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/red</item>
        <item name="colorSecondaryVariant">@color/redBlack</item>
        <item name="colorOnSecondary">@color/mainBackground</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/montserrat_bold</item>
    </style>

    <style name="splashScreen" parent="Theme.MovieAppJetpackSubmission2">
        <item name="android:windowBackground">@color/redBlack</item>
        <item name="android:statusBarColor">@color/redBlack</item>
    </style>

    <style name="detailApp" parent="Theme.MovieAppJetpackSubmission2">
        <item name="android:statusBarColor">@color/mainBackground</item>
    </style>
</resources>